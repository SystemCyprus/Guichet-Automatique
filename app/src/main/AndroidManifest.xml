<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mnf.collateral.guichetautomatique">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GuichetAutomatique">
        <activity android:name=".EcranListeEpargne"></activity>
        <activity android:name=".EcranListeCheques" />
        <activity android:name=".EcranListeClients" />
        <activity android:name=".EcranAdmin" />
        <activity android:name=".EcranPrincipal" />
        <activity android:name=".EcranLogin">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>